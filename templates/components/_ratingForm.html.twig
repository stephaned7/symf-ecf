<div class="container">
	<div class="row">
		<div class="col">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<input type="submit" value="{{ button_label }}">
			{{ form_end(form) }}
		</div>
	</div>
</div>
